import React, { Component } from 'react'

export class upcoming extends Component {
    render() {
        return (
            <div>
                show list then choose one if want to update delivery
            </div>
        )
    }
}

export default upcoming
